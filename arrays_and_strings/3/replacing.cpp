#include "replacing.h"

namespace arrays_and_structs {

void replacing(std::string& src, const std::string& to_replace, const std::string& replacer) {
    if(src.size() < to_replace.size())
        return;
}

}
